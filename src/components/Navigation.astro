<nav class="navigation">
  <input id="navigation-toggle" type="checkbox" />
  <label class="navigation__button" for="navigation-toggle">
    <span></span>
  </label>
  <ul class="navigation__list">
    <li><a href="#" class="navigation__link">About us</a></li>
    <li><a href="#" class="navigation__link">Our services</a></li>
    <li><a href="#" class="navigation__link">Contact</a></li>
  </ul>
</nav>

<style lang="scss">
  .navigation {
    &__list {
      position: fixed;
      visibility: hidden;
      top: 0;
      right: -100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 40px;
      width: 300px;
      height: 100%;
      padding: 100px 0;
      background-color: var(--secondary);
      transition-duration: 0.25s;
    }

    &__link,
    &__link:active,
    &__link:visited {
      color: var(--primary);
    }

    &__button {
      position: fixed;
      top: 20px;
      right: 20px;
      display: flex;
      align-items: center;
      width: 26px;
      height: 26px;

      cursor: pointer;
      z-index: 1;

      > span,
      > span::before,
      > span::after {
        display: block;
        position: absolute;
        width: 100%;
        height: 2px;
        background-color: var(--primary);
        transition: 0.2s;
      }
      > span::before {
        content: "";
        top: -8px;
      }
      > span::after {
        content: "";
        top: 8px;
      }
    }
  }

  #navigation-toggle {
    opacity: 0;
  }

  #navigation-toggle:checked ~ .navigation__button > span {
    transform: rotate(45deg);
  }
  #navigation-toggle:checked ~ .navigation__button > span::before {
    top: 0;
    transform: rotate(0);
  }
  #navigation-toggle:checked ~ .navigation__button > span::after {
    top: 0;
    transform: rotate(90deg);
  }
  #navigation-toggle:checked ~ .navigation__list {
    visibility: visible;
    right: 0;
  }

  @media (min-width: 768px) {
    .navigation {
      &__list {
        visibility: visible;
        right: 0;
        flex-direction: row;
        padding: 0 30px;
        width: 100%;
        height: 60px;
        justify-content: flex-end;
        background-color: transparent;
      }

      &__button {
        display: none;
      }
    }

    #navigation-toggle {
      display: none;
    }
  }
</style>
